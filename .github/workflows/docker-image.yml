name: Docker Image CI

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Log in to GHCR
      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # Log in to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      # Build once and tag for both registries
      - name: Build image
        run: |
          docker build \
            -t ghcr.io/kinola-iq/stock-analyst-backend:${{ github.sha }} \
            -t ghcr.io/kinola-iq/stock-analyst-backend:latest \
            -t kinola-iq/stock-analyst-backend:${{ github.sha }} \
            -t kinola-iq/stock-analyst-backend:latest \
            .

      # Push to GHCR
      - name: Push to GHCR
        run: |
          docker push ghcr.io/kinola-iq/stock-analyst-backend:${{ github.sha }}
          docker push ghcr.io/kinola-iq/stock-analyst-backend:latest

      # Push to Docker Hub
      - name: Push to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/stock-analyst-backend:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/stock-analyst-backend:latest